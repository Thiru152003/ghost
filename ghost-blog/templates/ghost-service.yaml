apiVersion: v1
kind: Service
metadata:
  name: ghost
  namespace: ghost-blog
spec:
  type: LoadBalancer  # ✅ Change from ClusterIP to LoadBalancer
  selector:
    app: ghost
  ports:
    - protocol: TCP
      port: 80
      targetPort: 2368  # ✅ Ensure this matches Ghost's actual container port
